package org.prog.web.pages.rozetka.widgets;

import org.openqa.selenium.WebDriver;

public class CatalogWidget {

    private final WebDriver driver;

    public CatalogWidget(WebDriver driver) {
        this.driver = driver;
    }
}
